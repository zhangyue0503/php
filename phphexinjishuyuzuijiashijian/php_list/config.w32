// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("php_list", "for php_list support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("php_list", "enable php_list support", "no");

if (PHP_PHP_LIST != "no") {
	EXTENSION("php_list", "php_list.c");
}

